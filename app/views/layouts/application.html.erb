<!DOCTYPE html>

<html>

  <head>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag 'application' %>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <%= csrf_meta_tag %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Chronos</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link">
            <% if signed_in? %>
            Welcome, <%= current_user.name %>
            <% end %>
          <span class="sr-only">(current)</span></a>
        </li>
      </ul>

      <ul class="navbar-nav ml-auto">

        <li class="nav-item">
          <% if !signed_in? && params[:sign_up] == 'patient' %>
            <%= link_to "Sign Up as Doctor", sign_up_path(sign_up:"doctor"), class: "nav-link"%>
          <% elsif !signed_in? %>
            <%= link_to "Sign Up as Patient", sign_up_path(sign_up:"patient"), class: "nav-link" %>
          <% end %>
        </li>

        <li class="nav-item" id="signinactive" >
          <% if signed_in? %>
            <%= button_to t(".sign_out"), sign_out_path, method: :delete, class: "nav-link btn btn-warning",style:"color:white" %>
          <% else -%>
            <%= link_to t(".sign_in"), sign_in_path, class: "nav-link" %>
          <% end -%>
        </li>
      
      </ul>
    </div>
  </nav>

    <%= yield %>
  </body>

</html>
